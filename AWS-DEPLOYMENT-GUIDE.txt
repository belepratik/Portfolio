AWS DEPLOYMENT GUIDE - 12 MONTH FREE TIER
==========================================

🎯 AWS STRATEGY: EC2 + RDS + S3 (Best Free Tier)
- Backend: EC2 t2.micro (750 hours/month free for 12 months)
- Database: RDS MySQL t2.micro (750 hours/month free for 12 months)  
- Frontend: S3 + CloudFront (5GB + 50GB transfer/month free)

📋 PREREQUISITES
----------------
1. AWS Account (free tier eligible)
2. AWS CLI installed
3. Basic AWS knowledge

⚡ STEP-BY-STEP AWS DEPLOYMENT
==============================

🔧 1. SETUP AWS CLI
-------------------
# Install AWS CLI
https://aws.amazon.com/cli/

# Configure credentials
aws configure
# Enter: Access Key ID, Secret Access Key, Region (us-east-1)

🗄️2. CREATE RDS MYSQL DATABASE (FREE TIER)
-------------------------------------------
# Go to AWS Console > RDS
1. Click "Create database"
2. Choose "MySQL" (8.0)
3. Template: "Free tier"
4. Settings:
   - DB instance identifier: portfolio-db
   - Master username: admin  
   - Master password: [create strong password]
5. DB instance class: db.t2.micro (FREE TIER)
6. Storage: 20 GiB (FREE TIER LIMIT)
7. Connectivity:
   - Public access: YES (for now)
   - Security group: Create new "portfolio-db-sg"
8. Additional configuration:
   - Initial database name: portfolio_db
9. Click "Create database"

🖥️ 3. DEPLOY BACKEND TO EC2 (FREE TIER)
---------------------------------------
# Launch EC2 Instance
1. Go to EC2 > Launch Instance
2. AMI: Amazon Linux 2
3. Instance type: t2.micro (FREE TIER)
4. Security group: 
   - SSH (22) from your IP
   - HTTP (80) from anywhere
   - Custom TCP (8080) from anywhere
5. Create/select key pair
6. Launch instance

# Connect and setup
ssh -i your-key.pem ec2-user@your-ec2-ip

# Install Java 17
sudo yum update -y
sudo amazon-linux-extras install java-openjdk17 -y

# Upload your JAR file
scp -i your-key.pem backend/target/*.jar ec2-user@your-ec2-ip:~/

# Create systemd service
sudo nano /etc/systemd/system/portfolio.service

[Unit]
Description=Portfolio App
After=network.target

[Service]
User=ec2-user
WorkingDirectory=/home/ec2-user
ExecStart=/usr/bin/java -jar portfolio-app.jar
Restart=always
Environment=DB_HOST=your-rds-endpoint
Environment=DB_USERNAME=admin
Environment=DB_PASSWORD=your-password
Environment=SERVER_PORT=8080

[Install]
WantedBy=multi-user.target

# Start service
sudo systemctl daemon-reload
sudo systemctl enable portfolio
sudo systemctl start portfolio

🌐 4. DEPLOY FRONTEND TO S3 (FREE TIER)
---------------------------------------
# Create S3 bucket
aws s3 mb s3://portfolio-app-[unique-name]

# Build React app with backend URL
cd frontend
echo "VITE_API_URL=http://your-ec2-ip:8080/api" > .env.production
npm run build

# Upload to S3
aws s3 sync dist/ s3://portfolio-app-[unique-name] --delete

# Enable static website hosting
aws s3 website s3://portfolio-app-[unique-name] \
  --index-document index.html \
  --error-document index.html

# Set public read policy
aws s3api put-bucket-policy \
  --bucket portfolio-app-[unique-name] \
  --policy '{
    "Version": "2012-10-17",
    "Statement": [{
      "Effect": "Allow",
      "Principal": "*",
      "Action": "s3:GetObject",
      "Resource": "arn:aws:s3:::portfolio-app-[unique-name]/*"
    }]
  }'

🚀 5. OPTIONAL: ADD CLOUDFRONT CDN (FREE TIER)
----------------------------------------------
# Create CloudFront distribution
aws cloudfront create-distribution \
  --distribution-config '{
    "CallerReference": "portfolio-'$(date +%s)'",
    "Origins": {
      "Quantity": 1,
      "Items": [{
        "Id": "S3-portfolio",
        "DomainName": "portfolio-app-[unique-name].s3.amazonaws.com",
        "S3OriginConfig": {
          "OriginAccessIdentity": ""
        }
      }]
    },
    "DefaultCacheBehavior": {
      "TargetOriginId": "S3-portfolio",
      "ViewerProtocolPolicy": "redirect-to-https",
      "TrustedSigners": {
        "Enabled": false,
        "Quantity": 0
      },
      "ForwardedValues": {
        "QueryString": false,
        "Cookies": {"Forward": "none"}
      }
    },
    "Comment": "Portfolio App CDN",
    "Enabled": true
  }'

📱 6. YOUR LIVE URLS
-------------------
Backend: http://your-ec2-ip:8080
Frontend: http://portfolio-app-[unique-name].s3-website-us-east-1.amazonaws.com
CDN: https://your-cloudfront-domain.cloudfront.net

💰 AWS FREE TIER BREAKDOWN (12 MONTHS)
======================================
✅ EC2 t2.micro: 750 hours/month (run 24/7 for free)
✅ RDS MySQL t2.micro: 750 hours/month + 20GB storage
✅ S3: 5GB storage + 50GB transfer/month  
✅ CloudFront: 50GB transfer/month

💡 COST AFTER FREE TIER
=======================
- EC2 t2.micro: ~$8.50/month
- RDS t2.micro: ~$15/month  
- S3 + CloudFront: ~$1-3/month
- **Total: ~$25-27/month**

🔒 SECURITY BEST PRACTICES
==========================
1. Restrict RDS security group to only EC2
2. Use IAM roles instead of access keys
3. Enable CloudTrail for monitoring
4. Regular security updates on EC2

⏱️ ESTIMATED SETUP TIME: 2-3 hours

🎯 WHY AWS IS BEST FOR YOUR PORTFOLIO
====================================
✅ 12-month free tier (vs 30-90 days elsewhere)
✅ Most mature ecosystem
✅ Best documentation and community
✅ Industry standard (great for resume)
✅ Scales with your growth